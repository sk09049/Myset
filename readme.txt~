problem description for array folder.
-------------------------------------
in this problem :there are three classes(java class) inthat each class able to store three students marks and their name.

from that list we have find the overall topper of three class(java class).

output:overall class topper name and his marks.

class names are:array1,array2,array3.

array3 have the main method.
